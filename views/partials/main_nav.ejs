<!--Nav Bar-->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/home">Assignment 2</a> <!--Main nav bar button to the left which directs users to the home-->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">

          <li class="nav-item"> <!--First nav bar button -->
          <% if (title == "Home") { %> <!--if else statement to highlight nav bar if on that section-->
            <a class="nav-link active" aria-current="page" href="/home#aboutme"><i class="fa-solid fa-house-chimney"></i> Home</a> <!--when button ic clicked takes user down to certain part in the same page. it also adds the icon image to nav bar-->
          <% } else { %>
            <a class="nav-link" aria-current="page" href="/home#aboutme">
          <% } %>
              <i class="fa-solid fa-house-chimney"></i> 
              Home</a>
          </li>

          <li class="nav-item">
          <% if (title == "About") { %>
            <a class="nav-link active" aria-current="page" href="/home#About"><i class="fa-solid fa-circle-info"></i> About</a>
            
          <% } else { %>
            <a class="nav-link" aria-current="page" href="/home#About">
          <% } %>
              <i class="fa-solid fa-circle-info"></i> 
              About</a>
          </li>

          <li class="nav-item">
          <% if (title == "Projects") { %>
            <a class="nav-link active" aria-current="page" href="/home#projects"><i class="fa-solid fa-box-open"></i> Projects</a>
          <% } else { %>
            <a class="nav-link" aria-current="page" href="/home#projects">
          <% } %>
            <i class="fa-solid fa-box-open"></i> Projects</a>
          </li>

          <li class="nav-item">
          <% if (title  == "Contact Us") { %>
            <a class="nav-link active" aria-current="page" href="/home#Contact"><i class="fa-solid fa-address-book"></i> Contact Us</a>
          <% } else { %>
            <a class="nav-link" aria-current="page" href="/home#Contact">
          <% } %>
              <i class="fa-solid fa-address-book"></i> 
              Contact Us</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>